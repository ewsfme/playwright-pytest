[pytest]
addopts = --alluredir=allure-results --tracing=retain-on-failure --screenshot=only-on-failure --reruns 1 --reruns-delay 3
testpaths = tests
markers =
    smoke: quick smoke checks
    regression: full regression suite
